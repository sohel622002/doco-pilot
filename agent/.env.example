# Credentials from Infrastructure / Servers page after you register a host
AGENT_KEY=
AGENT_SECRET=

# Used when running the agent with `node` on the host.
# When using `npm run dev` (Docker agent), compose overrides this to
# ws://host.docker.internal:3001 so the container can reach the local server.
BACKEND_WS_URL=ws://localhost:3001
